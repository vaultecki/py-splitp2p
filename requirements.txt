# SplitP2P – Python Dependencies
# Install with: pip install -r requirements.txt

# ── Pflicht ────────────────────────────────────────────────────────────────
# Ed25519-Signaturen und AES-256-GCM-Gruppenverschlüsselung
cryptography>=41.0

# ── P2P-Sync (empfohlen) ───────────────────────────────────────────────────
# GossipSub-Messaging und direkter Datei-Stream zwischen Peers.
# Ohne libp2p läuft die App im Offline-Modus (nur lokale Nutzung).
libp2p>=0.1.5

# ── Optional ───────────────────────────────────────────────────────────────
# Bildvorschau für Anhänge (JPG, PNG, GIF, WEBP) direkt im App-Fenster.
# Ohne Pillow werden Bilder im System-Viewer geöffnet.
# PDFs werden immer im System-Viewer geöffnet (kein Pillow nötig).
Pillow>=10.0

# ── Stdlib (kein Install nötig) ────────────────────────────────────────────
# tkinter   – GUI; auf Debian/Ubuntu ggf.: sudo apt install python3-tk
# sqlite3   – lokale Datenbank
# urllib    – Wechselkurs-Abruf
# asyncio   – asyncio-Loop für den P2P-Thread
# threading – Hintergrund-Thread für Netzwerk und Kurs-Fetch
