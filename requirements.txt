# SplitP2P – Python Dependencies
# Install with: pip install -r requirements.txt

# ── Pflicht ──────────────────────────────────────────────────────────────
cryptography>=41.0

# ── P2P-Sync (empfohlen) ─────────────────────────────────────────────────
# GossipSub, mDNS-Discovery, Bootstrap-Verbindungen
libp2p>=0.1.5
multiaddr>=0.0.9        # IPFS Bootstrap-Adressen parsen

# ── Optional ─────────────────────────────────────────────────────────────
# Bildvorschau für Anhänge direkt im App-Fenster (JPG, PNG, GIF, WEBP)
Pillow>=10.0

# ── Stdlib ───────────────────────────────────────────────────────────────
# tkinter   – GUI; auf Debian/Ubuntu: sudo apt install python3-tk
# sqlite3   – Datenbank
# urllib    – Wechselkurs-Abruf
# asyncio / threading – P2P-Hintergrundthread

# ── Charts ───────────────────────────────────────────────────────────────
matplotlib>=3.7          # Diagramme im Charts-Fenster

# ── PDF-Export ───────────────────────────────────────────────────────────
fpdf2>=2.7               # PDF-Generierung; CSV-Export funktioniert ohne
