# SplitP2P – Python Dependencies
# Install with: pip install -r requirements.txt

# ── Kern-Kryptografie & Sicherheit ───────────────────────────────────────
# Für Ed25519 Signaturen und SecretBox (Salsa20/Poly1305)
pynacl>=1.5.0
cryptography>=41.0

# ── P2P-Networking & Async ───────────────────────────────────────────────
# Trio ist das Fundament für die asynchrone Netzwerk-Logik
trio>=0.22.0
# P2P-Protokoll-Stack (GossipSub, mDNS, Streams)
py-libp2p>=0.2.0
multiaddr>=0.0.9

# ── GUI & UX (Optional, degradieren bei Fehlen) ──────────────────────────
# Bildvorschau für Belege
Pillow>=10.0
# Diagramme im Charts-Fenster
matplotlib>=3.7
# PDF-Export der Abrechnungen
fpdf2>=2.7
# QR-Code Generierung (für Invites/Backup)
qrcode[pil]>=7.4
# QR-Code Scan via Kamera
opencv-python>=4.8
